let wallpaper = "{{ wallpaper }}"
let background = "{{ background }}"
let foreground = "{{ foreground }}"
let cursor = "{{ cursor }}"

let color0  = "{{ color0 }}"
let color1  = "{{ color1 | lighten(0.15) | saturate(0.15) }}"
let color2  = "{{ color2 | lighten(0.08) | saturate(0.15) }}"
let color3  = "{{ color3 | lighten(0.08) | saturate(0.15) }}"
let color4  = "{{ color4 | lighten(0.08) | saturate(0.15) }}"
let color5  = "{{ color5 | lighten(0.08) | saturate(0.15) }}"
let color6  = "{{ color6 | lighten(0.08) | saturate(0.15) }}"
let color7  = "{{ color7 | lighten(0.05) }}"

let color8  = "{{ color8 | lighten(0.10) }}"
let color9  = "{{ color9  | lighten(0.15) | saturate(0.20) }}"
let color10 = "{{ color10 | lighten(0.15) | saturate(0.20) }}"
let color11 = "{{ color11 | lighten(0.15) | saturate(0.20) }}"
let color12 = "{{ color12 | lighten(0.15) | saturate(0.20) }}"
let color13 = "{{ color13 | lighten(0.15) | saturate(0.20) }}"
let color14 = "{{ color14 | lighten(0.15) | saturate(0.20) }}"
let color15 = "{{ color15 | lighten(0.10) }}"
