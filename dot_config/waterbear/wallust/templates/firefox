/* ==========================================================================
 * wallust.css
 * Generated by Wallust
 *
 * Primitive palette consumed by userChrome.css
 * ========================================================================== */

@layer tokens {
  :root {
    /* ------------------------------------------------------------
     * Gray / surface scale
     * ------------------------------------------------------------ */

    --primitive-gray-900: {{ background }};
    --primitive-gray-850: {{ background | lighten(0.05) }};
    --primitive-gray-800: {{ background | lighten(0.10) }};
    --primitive-gray-700: {{ background | lighten(0.15) }};
    --primitive-gray-600: {{ background | lighten(0.20) }};

    /* ------------------------------------------------------------
     * Text
     * ------------------------------------------------------------ */

    --primitive-white-90: {{ foreground }};
    --primitive-white-60: {{ foreground | darken(0.25) }};

    /* ------------------------------------------------------------
     * Accent
     * ------------------------------------------------------------ */

    --primitive-blue-400: {{ color12 }};

    /* ------------------------------------------------------------
     * Interaction alpha
     *
     * These remain neutral white overlays from the original
     * theme, rather than consuming palette colours.
     * ------------------------------------------------------------ */

    --primitive-alpha-08: rgba(255 255 255 / 0.08);
    --primitive-alpha-15: rgba(255 255 255 / 0.15);
    --primitive-alpha-20: rgba(255 255 255 / 0.20);
  }
}
